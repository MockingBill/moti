
<div class="layui-container">
    <div class="layui-row">
        <div class="layui-col-md6">
                <div class="layui-input-inline layui-form">
                    <select  id="page_menu_sysid" name="page_menu_sysid" lay-filter="sysSelect">
                    </select>
                </div>
        </div>
        <div class="layui-col-md6">
            <div class="layui-card">
                <div class="layui-card-header">下拉框选项动态添加</div>
                <br class="layui-card-body">
                    1.layui中引入jquery组件。 </br>
                    2.使用$("#select_id").append()方法把下拉框选项的html代码以追加的方式添加进去。</br>
                    3.然后使用form.render('select');方法重新渲染下拉框。
                </div>
            </div>
        </div>

    </div>
</div>

<script>

    layui.use(['form','jquery'], function(){
        var form = layui.form;
        var $ = layui.$;

        var items=[];
        for(var j=0;j<10;j++){
            items.push({
                id:j,
                text:'item'+j
            })
        }

        $('#page_menu_sysid').html('');

        for(var i in items){
            $('#page_menu_sysid').append('<option value =\"' + items[i].id + '\">' + items[i].text + '</option>');
            form.render('select');
            // form.render();
        }
        $('#page_menu_sysid').get(0).selectedIndex=0;

    });
</script>